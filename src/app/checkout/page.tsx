import { CheckoutView } from "@/components/CheckoutView";

export const metadata = {
  title: "Checkout | Tezz McKinnon est. 1996",
};

export default function CheckoutPage() {
  return <CheckoutView />;
}
